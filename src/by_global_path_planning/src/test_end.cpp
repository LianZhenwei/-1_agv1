#include "ros/ros.h"
#include "include/headfile.h"

int main(int argc, char  *argv[])
{
    // ros::init(argc, argv, "test_floor1");
    // ros::NodeHandle nt;

    // ros::param::set("floor",1);
    // ros::param::set("task",1);

    // ros::Publisher end_point_pub_test = nt.advertise<geometry_msgs::PoseStamped>("/move_base_simple/goal", 10);

    // geometry_msgs::PoseStamped msg;
    // msg.pose.position.x=13.04;
    // msg.pose.position.y=-3.323;
    // msg.pose.position.z = 0;

    // msg.pose.orientation.x = 0.03;
    // msg.pose.orientation.y = 0;
    // msg.pose.orientation.z = -0.709;
    // msg.pose.orientation.w = 0.7036;//-3.00544
    // for (int i = 0; i < 20; i++)
    // {
    //     end_point_pub_test.publish(msg);
    // }

   

    return 0;
}